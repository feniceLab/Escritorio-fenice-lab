# === ONLY track the escritorio/ Next.js app ===
# Ignore everything at root level by default
/*

# But keep these top-level items
!escritorio/
!.gitignore
!README.md
!deploy.sh
!webhook-server.js
!setup-local.sh

# Ignore inside escritorio (from child .gitignore)
